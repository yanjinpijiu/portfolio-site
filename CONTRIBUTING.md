# 项目规约

## 一、仓库边界：只放引擎，不放内容

**这个仓库是站点的「引擎」，不是站点的「内容」。**

引擎 = 代码、配置模板、部署脚本、文档。可以进仓库。

内容 = 本人的个人信息与站点数据。**一律不进仓库**，只在本地维护。

这条是硬约束，不是建议。原因很直接：仓库是公开的，而内容里有能定位到本人身份的东西。

### 绝对不能进仓库

| 类别 | 典型位置 | 说明 |
|---|---|---|
| 证件照、人脸 | `backend/src/main/resources/seed-images/avatar.*` | 任何含本人正脸的图片 |
| 证书、成绩单扫描件 | `backend/src/main/resources/seed-images/certs/` | 含姓名、证件号、准考证号、成绩报告单编号 |
| 个人联系方式 | 种子类里的姓名 / 手机号 / 邮箱 / 学校 | 见下 |
| 站点种子内容 | `backend/.../config/ContentSeeder.java`、`DataSeeder.java` | 真实资料与全部项目描述 |
| 各项目成果截图 | `backend/src/main/resources/seed-images/projects/` | 含项目界面与作品图 |
| 后台登录密钥 | `backend/application-local.yml` | 密钥只走环境变量 `APP_ADMIN_KEY` |
| 服务器公网 IP | 部署文档与脚本 | 一律写占位符；域名可以出现 |
| 真实访客 IP | 文档与截图 | 只能出现 RFC 5737 文档保留段 |
| 备案号 | 文档与截图 | 打码 |
| 运行数据 | `backend/data/`、`backend/backups/` | H2 库、上传的简历 PDF、备份 |

### 截图

`docs/screenshots/` 是 README 的配图，属于「演示」不是「内容」，可以进仓库，**但每次换图必须先脱敏**：人脸、手机号、邮箱、真实访客 IP、备案号一律打马赛克。

用马赛克而不是纯黑条——马赛克更像有意的脱敏，黑条看起来像图坏了。打码时注意绕开弹窗、浮层这类遮挡物，别把它们一起糊掉。改完把图重新看一遍再提交。

---

## 二、两个 head：项目与内容完全分开

仓库里有两个长期分支，职责不重叠：

| 分支 | 内容 | 去向 |
|---|---|---|
| `main` | **只有项目本身**，任何个人信息都不含 | 唯一推送的分支，对应公开仓库 |
| `content` | `main` + 本人的种子内容（Seeder 与 seed-images） | **仅本地**，永不推送 |

### 为什么这么分

站点内容主要活在数据库里（后台在线上直接改），代码里的种子只是首次启动的初始数据。所以「内容」在版本控制里的体量很小——就是几个文件加一批图片，正好可以完整地和项目剥离开。

### 日常怎么用

- **跑本地站点**（要有数据）：`git checkout content`
- **改引擎代码**：在 `main` 上改、提交，然后 `git checkout content && git rebase main` 把内容分支挪到新基底上
- **对外发布**：只推 `main`。**任何时候都不要 `git push --all`**，也不要把 `content` 合进 `main`

### 公开仓库首次启动是空的

这是有意为之：种子内容不在仓库里，所以别人克隆后首次启动站点没有任何数据。内容通过在后台录入，或把自己的 Seeder 放回本地。

---

## 三、提交前自检

三条都要跑，都干净才提交：

```bash
# 1. 有没有把内容文件加回去（下列路径命中即为违规）
git ls-files | grep -E "seed-images/|ContentSeeder|DataSeeder|application-local\.yml$|portfolio\.env$|\.pdf$"

# 2. 有没有手机号 / 邮箱混进跟踪文件
git ls-files -z | xargs -0 grep -nE "1[3-9][0-9]{9}"
git ls-files -z | xargs -0 grep -nE "[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}"

# 3. 有没有真实 IP（命中的应当只有文档保留段 192.0.2. / 198.51.100. / 203.0.113.）
git ls-files -z | xargs -0 grep -nE "([0-9]{1,3}\.){3}[0-9]{1,3}"
```

第 2 条会命中 `docs/` 里描述本规约的示例。**判断标准是：这句是不是在讲规则，而不是在讲某个人。** 前者留，后者删。
